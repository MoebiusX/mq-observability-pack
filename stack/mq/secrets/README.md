Dev-only credentials for the lab (`admin` / `app` → `passw0rd`). Committed on purpose:
the whole stack binds to loopback and is torn down with `docker compose down -v`.
Never reuse these files outside this repository.
