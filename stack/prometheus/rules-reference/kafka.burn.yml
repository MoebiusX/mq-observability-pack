# Copied by tools/reference-packs.mjs from https://github.com/MoebiusX/Observogram.git origin/develop (54223dd) reference-packs/rules/kafka.burn.yml — do not edit.
# GENERATED by tools/gen-burn-rules.mjs from reference-packs/kafka.pack.yaml (spec.policy) — do not edit.
# Multi-window burn-rate alerts (one per declared window), forecast alerts and the
# error-budget recording rules. Names/labels follow compile.mjs; tools/lib/burn-rules.mjs
# lists the five PromQL deviations. Sample step 30s, for: production.

groups:
  - name: kafka.errorbudget
    interval: 30s
    rules:
      - record: kafka:consumer_group_lag_seconds:error_ratio_5m
        expr: "(sum_over_time((max(kafka:consumer_lag:seconds_max_5m) > bool 60)[5m:30s]) / 10)"
        labels:
          sli: consumer_group_lag_seconds
          service: kafka
      - record: kafka:produce_latency_p99:error_ratio_5m
        expr: "(sum_over_time((max(kafka:produce_latency:p99_5m) > bool 0.1)[5m:30s]) / 10)"
        labels:
          sli: produce_latency_p99
          service: kafka
      - record: kafka:fetch_latency_p99:error_ratio_5m
        expr: "(sum_over_time((max(kafka:fetch_latency:p99_5m) > bool 0.05)[5m:30s]) / 10)"
        labels:
          sli: fetch_latency_p99
          service: kafka
      - record: kafka:controller_election_rate:error_ratio_5m
        expr: "(sum_over_time((max(kafka:controller_elections:rate_1h) > bool 1)[5m:60s]) / 5)"
        labels:
          sli: controller_election_rate
          service: kafka
      - record: kafka:errorbudget:burn_5m
        expr: "(sum(sum_over_time((1 - (up{job=\"kafka-broker\"} == bool 1))[5m:30s])) / ((count(up{job=\"kafka-broker\"})) * 10)) / 0.001"
        labels:
          slo: broker_availability_99_9
          sli: broker_availability
          service: kafka
      - record: kafka:errorbudget:burn_1h
        expr: "(sum(sum_over_time((1 - (up{job=\"kafka-broker\"} == bool 1))[1h:30s])) / ((count(up{job=\"kafka-broker\"})) * 120)) / 0.001"
        labels:
          slo: broker_availability_99_9
          sli: broker_availability
          service: kafka
      - record: kafka:errorbudget:burn_5m
        expr: "(sum(sum_over_time((1 - (kafka_topic_partition_in_sync_replica == bool kafka_topic_partition_replicas))[5m:30s])) / ((count(kafka_topic_partition_replicas)) * 10)) / 0.0005"
        labels:
          slo: partition_health_99_95
          sli: partition_replica_health
          service: kafka
      - record: kafka:errorbudget:burn_1h
        expr: "(sum(sum_over_time((1 - (kafka_topic_partition_in_sync_replica == bool kafka_topic_partition_replicas))[1h:30s])) / ((count(kafka_topic_partition_replicas)) * 120)) / 0.0005"
        labels:
          slo: partition_health_99_95
          sli: partition_replica_health
          service: kafka
      - record: kafka:errorbudget:burn_5m
        expr: "(sum_over_time((max(kafka:consumer_lag:seconds_max_5m) > bool 60)[5m:30s]) / 10) / 0.01"
        labels:
          slo: consumer_lag_99_under_60s
          sli: consumer_group_lag_seconds
          service: kafka
      - record: kafka:errorbudget:burn_1h
        expr: "(sum_over_time((max(kafka:consumer_lag:seconds_max_5m) > bool 60)[1h:30s]) / 120) / 0.01"
        labels:
          slo: consumer_lag_99_under_60s
          sli: consumer_group_lag_seconds
          service: kafka
      - record: kafka:errorbudget:burn_5m
        expr: "(sum_over_time((max(kafka:produce_latency:p99_5m) > bool 0.1)[5m:30s]) / 10) / 0.01"
        labels:
          slo: produce_latency_99_p99_100ms
          sli: produce_latency_p99
          service: kafka
      - record: kafka:errorbudget:burn_1h
        expr: "(sum_over_time((max(kafka:produce_latency:p99_5m) > bool 0.1)[1h:30s]) / 120) / 0.01"
        labels:
          slo: produce_latency_99_p99_100ms
          sli: produce_latency_p99
          service: kafka
      - record: kafka:errorbudget:burn_5m
        expr: "(sum_over_time((max(kafka:fetch_latency:p99_5m) > bool 0.05)[5m:30s]) / 10) / 0.01"
        labels:
          slo: fetch_latency_99_p99_50ms
          sli: fetch_latency_p99
          service: kafka
      - record: kafka:errorbudget:burn_1h
        expr: "(sum_over_time((max(kafka:fetch_latency:p99_5m) > bool 0.05)[1h:30s]) / 120) / 0.01"
        labels:
          slo: fetch_latency_99_p99_50ms
          sli: fetch_latency_p99
          service: kafka
      - record: kafka:errorbudget:burn_5m
        expr: "(sum_over_time((max(kafka:controller_elections:rate_1h) > bool 1)[5m:60s]) / 5) / 0.01"
        labels:
          slo: controller_stability_99_under_1ph
          sli: controller_election_rate
          service: kafka
      - record: kafka:errorbudget:burn_1h
        expr: "(sum_over_time((max(kafka:controller_elections:rate_1h) > bool 1)[1h:60s]) / 60) / 0.01"
        labels:
          slo: controller_stability_99_under_1ph
          sli: controller_election_rate
          service: kafka
  - name: kafka_broker_availability_99_9_burn
    interval: 30s
    rules:
      - alert: broker_availability_99_9_burn_14x_5m_1h
        expr: |
          (
            (sum(sum_over_time((1 - (up{job="kafka-broker"} == bool 1))[5m:30s])) / ((count(up{job="kafka-broker"})) * 10)) > 0.014
          ) and (
            (sum(sum_over_time((1 - (up{job="kafka-broker"} == bool 1))[1h:30s])) / ((count(up{job="kafka-broker"})) * 120)) > 0.014
          ) and (
            sum(sum_over_time((1 - (up{job="kafka-broker"} == bool 1))[5m:30s])) >= 2
          )
        for: 2m
        labels:
          severity: SEV1
          pack: kafka
          slo: broker_availability_99_9
          sli: broker_availability
          service: kafka
          burn_rate: "14"
          window_short: 5m
          window_long: 1h
        annotations:
          summary: "Burn rate 14x on broker_availability_99_9"
          description: "Both the 5m and 1h error ratios exceed 14x of the 0.1% error budget for broker_availability_99_9 (objective 99.9% over 30d), with at least 2 bad samples in the 5m window."
          slo_objective: 99.9%
          slo_window: 30d
      - alert: broker_availability_99_9_burn_6x_30m_6h
        expr: |
          (
            (sum(sum_over_time((1 - (up{job="kafka-broker"} == bool 1))[30m:30s])) / ((count(up{job="kafka-broker"})) * 60)) > 0.006
          ) and (
            (sum(sum_over_time((1 - (up{job="kafka-broker"} == bool 1))[6h:30s])) / ((count(up{job="kafka-broker"})) * 720)) > 0.006
          ) and (
            sum(sum_over_time((1 - (up{job="kafka-broker"} == bool 1))[30m:30s])) >= 2
          )
        for: 5m
        labels:
          severity: SEV2
          pack: kafka
          slo: broker_availability_99_9
          sli: broker_availability
          service: kafka
          burn_rate: "6"
          window_short: 30m
          window_long: 6h
        annotations:
          summary: "Burn rate 6x on broker_availability_99_9"
          description: "Both the 30m and 6h error ratios exceed 6x of the 0.1% error budget for broker_availability_99_9 (objective 99.9% over 30d), with at least 2 bad samples in the 30m window."
          slo_objective: 99.9%
          slo_window: 30d
  - name: kafka_partition_health_99_95_burn
    interval: 30s
    rules:
      - alert: partition_health_99_95_burn_14x_5m_1h
        expr: |
          (
            (sum(sum_over_time((1 - (kafka_topic_partition_in_sync_replica == bool kafka_topic_partition_replicas))[5m:30s])) / ((count(kafka_topic_partition_replicas)) * 10)) > 0.007
          ) and (
            (sum(sum_over_time((1 - (kafka_topic_partition_in_sync_replica == bool kafka_topic_partition_replicas))[1h:30s])) / ((count(kafka_topic_partition_replicas)) * 120)) > 0.007
          ) and (
            sum(sum_over_time((1 - (kafka_topic_partition_in_sync_replica == bool kafka_topic_partition_replicas))[5m:30s])) >= 2
          )
        for: 2m
        labels:
          severity: SEV1
          pack: kafka
          slo: partition_health_99_95
          sli: partition_replica_health
          service: kafka
          burn_rate: "14"
          window_short: 5m
          window_long: 1h
        annotations:
          summary: "Burn rate 14x on partition_health_99_95"
          description: "Both the 5m and 1h error ratios exceed 14x of the 0.05% error budget for partition_health_99_95 (objective 99.95% over 30d), with at least 2 bad samples in the 5m window."
          slo_objective: 99.95%
          slo_window: 30d
      - alert: partition_health_99_95_burn_6x_30m_6h
        expr: |
          (
            (sum(sum_over_time((1 - (kafka_topic_partition_in_sync_replica == bool kafka_topic_partition_replicas))[30m:30s])) / ((count(kafka_topic_partition_replicas)) * 60)) > 0.003
          ) and (
            (sum(sum_over_time((1 - (kafka_topic_partition_in_sync_replica == bool kafka_topic_partition_replicas))[6h:30s])) / ((count(kafka_topic_partition_replicas)) * 720)) > 0.003
          ) and (
            sum(sum_over_time((1 - (kafka_topic_partition_in_sync_replica == bool kafka_topic_partition_replicas))[30m:30s])) >= 2
          )
        for: 5m
        labels:
          severity: SEV2
          pack: kafka
          slo: partition_health_99_95
          sli: partition_replica_health
          service: kafka
          burn_rate: "6"
          window_short: 30m
          window_long: 6h
        annotations:
          summary: "Burn rate 6x on partition_health_99_95"
          description: "Both the 30m and 6h error ratios exceed 6x of the 0.05% error budget for partition_health_99_95 (objective 99.95% over 30d), with at least 2 bad samples in the 30m window."
          slo_objective: 99.95%
          slo_window: 30d
  - name: kafka_produce_latency_99_p99_100ms_burn
    interval: 30s
    rules:
      - alert: produce_latency_99_p99_100ms_burn_14x_5m_1h
        expr: |
          (
            (sum_over_time((max(kafka:produce_latency:p99_5m) > bool 0.1)[5m:30s]) / 10) > 0.14
          ) and (
            (sum_over_time((max(kafka:produce_latency:p99_5m) > bool 0.1)[1h:30s]) / 120) > 0.14
          ) and (
            sum_over_time((max(kafka:produce_latency:p99_5m) > bool 0.1)[5m:30s]) >= 2
          )
        for: 2m
        labels:
          severity: SEV1
          pack: kafka
          slo: produce_latency_99_p99_100ms
          sli: produce_latency_p99
          service: kafka
          burn_rate: "14"
          window_short: 5m
          window_long: 1h
        annotations:
          summary: "Burn rate 14x on produce_latency_99_p99_100ms"
          description: "Both the 5m and 1h error ratios exceed 14x of the 1% error budget for produce_latency_99_p99_100ms (objective 99% over 30d), with at least 2 bad samples in the 5m window."
          slo_objective: 99%
          slo_window: 30d
      - alert: produce_latency_99_p99_100ms_burn_6x_30m_6h
        expr: |
          (
            (sum_over_time((max(kafka:produce_latency:p99_5m) > bool 0.1)[30m:30s]) / 60) > 0.06
          ) and (
            (sum_over_time((max(kafka:produce_latency:p99_5m) > bool 0.1)[6h:30s]) / 720) > 0.06
          ) and (
            sum_over_time((max(kafka:produce_latency:p99_5m) > bool 0.1)[30m:30s]) >= 2
          )
        for: 5m
        labels:
          severity: SEV2
          pack: kafka
          slo: produce_latency_99_p99_100ms
          sli: produce_latency_p99
          service: kafka
          burn_rate: "6"
          window_short: 30m
          window_long: 6h
        annotations:
          summary: "Burn rate 6x on produce_latency_99_p99_100ms"
          description: "Both the 30m and 6h error ratios exceed 6x of the 1% error budget for produce_latency_99_p99_100ms (objective 99% over 30d), with at least 2 bad samples in the 30m window."
          slo_objective: 99%
          slo_window: 30d
  - name: kafka_fetch_latency_99_p99_50ms_burn
    interval: 30s
    rules:
      - alert: fetch_latency_99_p99_50ms_burn_14x_5m_1h
        expr: |
          (
            (sum_over_time((max(kafka:fetch_latency:p99_5m) > bool 0.05)[5m:30s]) / 10) > 0.14
          ) and (
            (sum_over_time((max(kafka:fetch_latency:p99_5m) > bool 0.05)[1h:30s]) / 120) > 0.14
          ) and (
            sum_over_time((max(kafka:fetch_latency:p99_5m) > bool 0.05)[5m:30s]) >= 2
          )
        for: 2m
        labels:
          severity: SEV1
          pack: kafka
          slo: fetch_latency_99_p99_50ms
          sli: fetch_latency_p99
          service: kafka
          burn_rate: "14"
          window_short: 5m
          window_long: 1h
        annotations:
          summary: "Burn rate 14x on fetch_latency_99_p99_50ms"
          description: "Both the 5m and 1h error ratios exceed 14x of the 1% error budget for fetch_latency_99_p99_50ms (objective 99% over 30d), with at least 2 bad samples in the 5m window."
          slo_objective: 99%
          slo_window: 30d
      - alert: fetch_latency_99_p99_50ms_burn_6x_30m_6h
        expr: |
          (
            (sum_over_time((max(kafka:fetch_latency:p99_5m) > bool 0.05)[30m:30s]) / 60) > 0.06
          ) and (
            (sum_over_time((max(kafka:fetch_latency:p99_5m) > bool 0.05)[6h:30s]) / 720) > 0.06
          ) and (
            sum_over_time((max(kafka:fetch_latency:p99_5m) > bool 0.05)[30m:30s]) >= 2
          )
        for: 5m
        labels:
          severity: SEV2
          pack: kafka
          slo: fetch_latency_99_p99_50ms
          sli: fetch_latency_p99
          service: kafka
          burn_rate: "6"
          window_short: 30m
          window_long: 6h
        annotations:
          summary: "Burn rate 6x on fetch_latency_99_p99_50ms"
          description: "Both the 30m and 6h error ratios exceed 6x of the 1% error budget for fetch_latency_99_p99_50ms (objective 99% over 30d), with at least 2 bad samples in the 30m window."
          slo_objective: 99%
          slo_window: 30d
  - name: kafka_consumer_lag_99_under_60s_burn
    interval: 30s
    rules:
      - alert: consumer_lag_99_under_60s_burn_10x_10m_1h
        expr: |
          (
            (sum_over_time((max(kafka:consumer_lag:seconds_max_5m) > bool 60)[10m:30s]) / 20) > 0.1
          ) and (
            (sum_over_time((max(kafka:consumer_lag:seconds_max_5m) > bool 60)[1h:30s]) / 120) > 0.1
          ) and (
            sum_over_time((max(kafka:consumer_lag:seconds_max_5m) > bool 60)[10m:30s]) >= 2
          )
        for: 5m
        labels:
          severity: SEV2
          pack: kafka
          slo: consumer_lag_99_under_60s
          sli: consumer_group_lag_seconds
          service: kafka
          burn_rate: "10"
          window_short: 10m
          window_long: 1h
        annotations:
          summary: "Burn rate 10x on consumer_lag_99_under_60s"
          description: "Both the 10m and 1h error ratios exceed 10x of the 1% error budget for consumer_lag_99_under_60s (objective 99% over 7d), with at least 2 bad samples in the 10m window."
          slo_objective: 99%
          slo_window: 7d
      - alert: consumer_lag_99_under_60s_burn_4x_1h_6h
        expr: |
          (
            (sum_over_time((max(kafka:consumer_lag:seconds_max_5m) > bool 60)[1h:30s]) / 120) > 0.04
          ) and (
            (sum_over_time((max(kafka:consumer_lag:seconds_max_5m) > bool 60)[6h:30s]) / 720) > 0.04
          ) and (
            sum_over_time((max(kafka:consumer_lag:seconds_max_5m) > bool 60)[1h:30s]) >= 2
          )
        for: 10m
        labels:
          severity: SEV3
          pack: kafka
          slo: consumer_lag_99_under_60s
          sli: consumer_group_lag_seconds
          service: kafka
          burn_rate: "4"
          window_short: 1h
          window_long: 6h
        annotations:
          summary: "Burn rate 4x on consumer_lag_99_under_60s"
          description: "Both the 1h and 6h error ratios exceed 4x of the 1% error budget for consumer_lag_99_under_60s (objective 99% over 7d), with at least 2 bad samples in the 1h window."
          slo_objective: 99%
          slo_window: 7d
  - name: kafka_controller_stability_99_under_1ph_burn
    interval: 30s
    rules:
      - alert: controller_stability_99_under_1ph_burn_8x_15m_2h
        expr: |
          (
            (sum_over_time((max(kafka:controller_elections:rate_1h) > bool 1)[15m:60s]) / 15) > 0.08
          ) and (
            (sum_over_time((max(kafka:controller_elections:rate_1h) > bool 1)[2h:60s]) / 120) > 0.08
          ) and (
            sum_over_time((max(kafka:controller_elections:rate_1h) > bool 1)[15m:60s]) >= 2
          )
        for: 5m
        labels:
          severity: SEV2
          pack: kafka
          slo: controller_stability_99_under_1ph
          sli: controller_election_rate
          service: kafka
          burn_rate: "8"
          window_short: 15m
          window_long: 2h
        annotations:
          summary: "Burn rate 8x on controller_stability_99_under_1ph"
          description: "Both the 15m and 2h error ratios exceed 8x of the 1% error budget for controller_stability_99_under_1ph (objective 99% over 7d), with at least 2 bad samples in the 15m window."
          slo_objective: 99%
          slo_window: 7d
      - alert: controller_stability_99_under_1ph_burn_3x_1h_6h
        expr: |
          (
            (sum_over_time((max(kafka:controller_elections:rate_1h) > bool 1)[1h:60s]) / 60) > 0.03
          ) and (
            (sum_over_time((max(kafka:controller_elections:rate_1h) > bool 1)[6h:60s]) / 360) > 0.03
          ) and (
            sum_over_time((max(kafka:controller_elections:rate_1h) > bool 1)[1h:60s]) >= 2
          )
        for: 10m
        labels:
          severity: SEV3
          pack: kafka
          slo: controller_stability_99_under_1ph
          sli: controller_election_rate
          service: kafka
          burn_rate: "3"
          window_short: 1h
          window_long: 6h
        annotations:
          summary: "Burn rate 3x on controller_stability_99_under_1ph"
          description: "Both the 1h and 6h error ratios exceed 3x of the 1% error budget for controller_stability_99_under_1ph (objective 99% over 7d), with at least 2 bad samples in the 1h window."
          slo_objective: 99%
          slo_window: 7d
  - name: kafka.forecast
    interval: 1m
    rules:
      - alert: broker_availability_99_9_forecast_breach
        expr: "predict_linear(kafka:errorbudget:burn_1h{slo=\"broker_availability_99_9\"}[1d], 86400) > 1 and min_over_time(kafka:errorbudget:burn_1h{slo=\"broker_availability_99_9\"}[2h]) > 1"
        for: 15m
        labels:
          severity: SEV2
          pack: kafka
          slo: broker_availability_99_9
          sli: broker_availability
          service: kafka
          kind: forecast
        annotations:
          summary: "broker_availability_99_9 has burned faster than its budget for 2h and the trend projects a breach"
          method: "linear on the 1h burn rate (pack declares holt-winters; the lab implements linear only)"
          horizon: "1d evaluated (pack declares 7d; capped at the 1d regression window, deviation 5)"
          horizon_declared: 7d
          on_projected_breach: open_ticket
      - alert: partition_health_99_95_forecast_breach
        expr: "predict_linear(kafka:errorbudget:burn_1h{slo=\"partition_health_99_95\"}[1d], 86400) > 1 and min_over_time(kafka:errorbudget:burn_1h{slo=\"partition_health_99_95\"}[2h]) > 1"
        for: 15m
        labels:
          severity: SEV2
          pack: kafka
          slo: partition_health_99_95
          sli: partition_replica_health
          service: kafka
          kind: forecast
        annotations:
          summary: "partition_health_99_95 has burned faster than its budget for 2h and the trend projects a breach"
          method: "linear on the 1h burn rate (pack declares holt-winters; the lab implements linear only)"
          horizon: "1d evaluated (pack declares 7d; capped at the 1d regression window, deviation 5)"
          horizon_declared: 7d
          on_projected_breach: open_ticket
      - alert: consumer_lag_99_under_60s_forecast_breach
        expr: "predict_linear(kafka:errorbudget:burn_1h{slo=\"consumer_lag_99_under_60s\"}[1d], 86400) > 1 and min_over_time(kafka:errorbudget:burn_1h{slo=\"consumer_lag_99_under_60s\"}[2h]) > 1"
        for: 15m
        labels:
          severity: SEV3
          pack: kafka
          slo: consumer_lag_99_under_60s
          sli: consumer_group_lag_seconds
          service: kafka
          kind: forecast
        annotations:
          summary: "consumer_lag_99_under_60s has burned faster than its budget for 2h and the trend projects a breach"
          method: "linear on the 1h burn rate (pack declares percentile-of-history; the lab implements linear only)"
          horizon: "1d evaluated (pack declares 3d; capped at the 1d regression window, deviation 5)"
          horizon_declared: 3d
          on_projected_breach: post_warning
